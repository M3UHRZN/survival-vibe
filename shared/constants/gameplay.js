export const WORLD_LIMIT = 32;
export const BASE_ACTION_INTERVAL = 0.58;
export const INTERACTION_RANGE = 2.2;
export const PLAYER_BASE_MOVE_SPEED = 5.2;
export const SPRINT_MULTIPLIER = 1.3;

export const MOVE_VECTOR_FORWARD = Object.freeze([
  -Math.SQRT1_2,
  0,
  -Math.SQRT1_2,
]);

export const MOVE_VECTOR_RIGHT = Object.freeze([
  Math.SQRT1_2,
  0,
  -Math.SQRT1_2,
]);
